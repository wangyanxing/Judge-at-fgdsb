-- Definition for a binary tree node
-- TreeNode = {
--     new = function(v)
--         return {val = v, left = nil, right = nil}
--     end
-- }

-- @param root, TreeNode
-- @param m,n, integer
-- @return TreeNode
function lca(root, m, n)
end